query FormsShortView {
  forms {
    id
    description
    title
  }
}

query FormById($id: ID!) {
  form(id: $id) {
    id
    title
    description
    questions {
      id
      title
      type
      options
    }
  }
}
